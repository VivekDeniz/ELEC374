module control_unit_tb();
		reg clk,reset,stop, run;
		reg [31:0] inport_data;
		reg[31:0] outport_data;
	 


	 
	 
	 
	 
	 
	 

datapath datapath(
    .clk(clk),.reset(reset),.stop(stop),
	 .inport_data(inport_data),
	 .outport_data(outport_data),
	 .run(run)
	 
	 
);